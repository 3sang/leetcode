import Index from "./index.md";

// 选择链表下的题目序列。
const listType = (no)=>{
  switch (no) {
    case "0":
      return Index;
    default:
      return Index;
  }
}

export default listType;
